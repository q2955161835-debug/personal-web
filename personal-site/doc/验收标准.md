# 验收标准

## 测试环境
- 本地项目目录：`D:\0文件夹\个人站\personal-site`
- 开发服务：`npm run dev -- -p 3000`
- 验证浏览器：应用内 Browser 或等效 Playwright 浏览器
- 必跑命令：`npm run lint`、`npm run build`
- 视觉验收视口：桌面 1440x900 附近、移动 390x844 附近

## 全局浏览器纵览验收

### 验收步骤
1. 打开 `http://localhost:3000`。
2. 从 Hero 首屏开始，慢速滚动经过 About、Projects、Data Analysis、Experience、Contact 到页面底部。
3. 从 Contact 向上滚回 Projects、About 和 Hero。
4. 在滚动期间截图或观察关键状态，并检查浏览器控制台新增错误。

### 预期结果
- Hero 粒子在首屏正常显示，从 Projects 回滚到上方页面后不消失。
- About 内容与全局 Canvas 不互相遮挡，页面上下没有大段失衡空白。
- Projects 段进入后使用独立 DNA 视觉，Hero 粒子淡出，不再干扰 DNA。
- 滚动过渡连续自然，项目切换节奏跟随鼠标滚轮速度，不出现离散跳变过快。
- Data Analysis、Experience、Contact 段上下衔接紧凑，不出现大段空白或内容漂浮失衡。
- Canvas 始终非空，无主要视觉对象飞出视口或遮挡正文。
- Hero 标题仅保留文字自身流光渐变，不出现横向扫光条或灰色光板。
- 顶部导航为无黑色框的线性显示，当前项通过颜色和下划线标识。
- Projects 后续统一背景必须有可见但不喧宾夺主的流动渐变与 5 个漂浮光晕，滚动到 Data、Timeline、Contact 时不能退回纯黑背景。
- Data、Timeline 的主要标题、说明、指标、标签和底部导航必须静态可读，不出现乱码占位或加载失败字符；Contact 联系文字可保留进入视口后的逐字入场效果。
- 全站鼠标视觉统一为淡彩虹折射环：只保留轻微彩虹环和外晕，不得出现准星、实体暗圈、强涟漪、横向扫光条或放大镜式黑边；主要交互元素 hover 后仍有可见反馈。

### 结果记录
- 2026-07-05：通过。`npm run lint`、`npm run build` 均通过；Edge Chromium + Playwright 完成桌面 1440x900 与移动 390x844 视觉验收，证据位于 `try/visual-qa-20260705-215203/`。应用内 Browser 在读取页面状态时超时重置，已记录为工具异常并改用等效浏览器自动化验收。
- 2026-07-05：本轮通过。`npm run lint`、`npm run build` 均通过；Edge Chromium + Playwright 完成 Hero→About→Projects→Data Analysis→Experience→Contact→回滚 About 的全局纵览，最终证据位于 `try/visual-qa-20260705-222843/`，`result.json` 中所有 pass 项为 true，控制台运行错误为空。
- 2026-07-05：本轮通过。应用内 Browser 完成 Hero、About、Projects、项目详情、Data Analysis、Timeline、Contact 的桌面视觉与交互抽检；`npm run lint`、`npm run build` 均通过。Hero 横向扫光条已移除，About 中 `6B+ 历史消耗` 与正文一致。
- 2026-07-06：本轮通过。`npm run lint`、`npm run build` 均通过；应用内 Browser 可连接但长页面 WebGL 截图阶段超时，改用 Playwright 完成桌面与移动全局纵览。证据位于 `try/visual-qa-20260705-2343/`，`result.json` 显示控制台错误为空、移动端无横向溢出、导航无黑色圆角框、全站无 `范俊杰`/`苏州`/`93+` 残留。
- 2026-07-06：本轮通过。`npm run lint`、`npm run build` 均通过；Playwright 完成桌面 1440x900 与移动 390x844 全局纵览，证据位于 `try/visual-qa-20260706-dna-data-contact/`。`result.json` 显示 `passed=true`、控制台错误为空、桌面/移动端无横向溢出、彩虹鼠标存在且无旧准星类。
- 2026-07-05：本轮通过。`npm run lint`、`npm run build` 均通过；Playwright 完成桌面 1440x900 与移动 390x844 全局纵览，证据位于 `try/visual-qa-final-1783281556123/`，定点复核位于 `try/visual-qa-final-check-1783281780074/`。`result.json` 显示 `passed=true`、控制台错误为空、无横向溢出、Next dev indicator 已隐藏、Timeline sticky 恢复、Contact 陨石可见。
- 2026-07-06：本轮通过。优化前提交为 `1c80755`，文件级备份位于 `D:\0文件夹\备份\personal-site-pre-performance-20260706-0406\personal-site-1c80755.zip`；`npm run lint`、`npm run build` 均通过。Playwright 完成桌面 1440x900 与移动 390x844 全局纵览，证据位于 `try/visual-qa-20260706-performance/`，`result-filtered.json` 显示 `passed=true`、控制台错误为空、桌面/移动端无横向溢出。应用内 Browser 的 DOM 快照接口异常已记录，改用等效 Playwright 验收。
- 2026-07-06：本轮通过。`npm run lint`、`npm run build` 均通过；应用内 Browser 可连接本地页面并读取日志，截图阶段因长页面 WebGL 时序不稳定改用 Playwright 完整复核。证据位于 `try/visual-qa-20260706-dna-fade-scatter/`，`result.json` 显示 `passed=true`、`consoleErrors=[]`、桌面与移动端无横向溢出，Data 起点 Projects HUD 与右侧序号导航透明度均为 0。
- 2026-07-06：本轮通过。`npm run lint`、`npm run build` 均通过；应用内 Browser 可连接页面但 DOM 快照接口返回旧异常，改用 Playwright 完成桌面与移动视觉验收。证据位于 `try/visual-qa-20260706-data-transitions/`，`result.json` 显示 `passed=true`、`consoleErrors=[]`、3 个背景光斑可见、Data 坐标轴稳定、Timeline/Contact 内容淡入通过、桌面与移动端无横向溢出。
- 2026-07-06：本轮通过。背景光晕增强为 5 个漂浮光斑，Data/Timeline/Contact 文本改为进入视口后触发的“乱码成字 + 逐字错峰上滑”，并后移 Data 及以下 section 淡入触发点。`npm run lint` 与 `npm run build` 均通过；应用内 Browser 基础检查通过但 `domSnapshot` 仍为旧接口异常，Playwright 证据位于 `try/visual-qa-20260706-text-glow/`，`result.json` 显示 `passed=true`、`glowVisible=true`、`textRevealStarts=true`、`dataFadeIn=true`、`timelineFadeIn=true`、`contactFadeIn=true`、桌面与移动端无横向溢出。
- 2026-07-06：本轮通过。作品集展示文案增强，About 统计更新为 `30M+ 代码行数` 与 `16B+ 历史消耗 token`；Data/Timeline 改为静态可读文案，移除临时乱码占位。`npm run lint`、`npm run build` 均通过；Playwright 证据位于 `try/visual-qa-content-20260706-124502/`，`result.json` 显示 `passed=true`、桌面/移动 `consoleErrors=[]`、无横向溢出、无旧文案残留。

## Projects DNA 项目段验收

### 验收步骤
1. 滚动到 Projects 段。
2. 慢速滚动观察 10 个项目的切换。
3. 点击右侧项目序号跳转到首尾与中间项目。
4. 点击 DNA 键标签或右侧项目序号，检查是否定位到对应项目。
5. 从 Projects 上划回 About，再从 About 下滑进入 Projects，观察 DNA 是否反向消散/重新聚合。
6. 观察项目标签、右侧项目说明、DNA 主体、背景粒子关系。

### 预期结果
- 项目名称不重叠，DNA 键位贴近对应项目位置，不堆在底部。
- 左侧状态栏只显示章节与当前序号，不出现“当前 DNA 键位”旧描述，不重复显示 DNA 键位上的项目标题和副标题。
- 右侧序号导航可点击，激活态与当前项目一致。
- DNA 键标签跟随同一螺旋变换，位置固定在对应键附近，点击标签可平滑定位到该项目。
- 当前项目说明直接固定在右侧，展示项目定位、技术栈、标签和 GitHub 链接，随滚动切换淡入淡出，不再打开旧项目详情弹层。
- Lang Drill Agent 必须在右侧说明中展示 Demo 入口，且 Demo 与 GitHub 可分别打开。
- DNA 主体足够长，首个项目键位进入时已经位于 DNA 结构上，不作为过渡散点使用。
- DNA 上端必须有 0 号空槽段用于粒子聚合动画，0 号段没有项目内容；第 1 个真实项目只能在聚合完成后进入居中展示。
- DNA 末端需要保留到第 11 个空槽的位置；第 10 个真实项目必须有与其他项目接近的完整居中展示长度。
- 从 About 下滑进入 Projects 时，粒子由散点聚合成 DNA；从 Projects 上划回 About 时，DNA 反向消散且不残留干扰 About。
- DNA 除项目粗碱基对外，必须有较细碱基对填充，背骨线条有宽度但不能固定点过曝。
- 相机角度保持稳定，滚动驱动 DNA 自身旋转、向下移动、粒子聚合与项目键缩放。
- 鼠标划过 DNA 粒子时粒子被打散，鼠标移开后回弹复原。
- DNA 主体清晰，后景粒子数量克制，不干扰项目浏览。
- 从 Projects 切回上方页面后，首页粒子和相机状态恢复正常。
- 右侧说明与 DNA 标签不能放重复大段内容；DNA 内部编号标记可作为键位提示，但不能遮挡项目名。
- 右侧说明切换必须滞后于 DNA 键位运动：当前项目键先到达居中焦点，右侧说明再切换为该项目。

### 结果记录
- 2026-07-05：通过。桌面截图 `02-desktop-projects-flow.png` 显示 Hero 粒子已与 DNA 段分离、后景粒子克制、项目标签固定在 DNA 附近；移动截图 `07-mobile-projects-flow.png` 显示移动端仅保留左侧标题与序号，项目名由 DNA 标签承载，无文字重叠。滚轮样本从项目 1 渐进到项目 2、项目 3，未出现一次滚动直接跳到末尾。
- 2026-07-05：本轮通过。最终截图 `03-desktop-dna-gather.png`、`05-desktop-dna-mid-active-key.png`、`15-mobile-dna-projects.png` 显示粒子由项目入口聚合到 DNA、相机角度稳定、键标签跟随 DNA、移动端中心项目按钮不与左侧状态栏重叠；`06-desktop-dna-pointer-scatter.png` 与 `07-desktop-dna-recovered.png` 覆盖鼠标打散与复原。
- 2026-07-05：本轮通过。应用内 Browser 验证 Projects 首个项目进入时 DNA 已聚合成型，Hero 粒子不再作为项目背景；稳定 DOM 玻璃入口可打开详情，DNA 内部编号不与项目名重叠；粒子密度提高后单粒尺寸与 Bloom 已下调，未再形成大面积白光遮挡。
- 2026-07-06：本轮通过。Playwright 截图 `03-desktop-projects-start.png`、`04-desktop-projects-mid.png`、`13-mobile-projects.png` 显示 DNA 加长、细碱基对填充、项目键贴近 DNA；`result.json` 中 `projectButtons=7`、`projectDetailOpened=true`，项目详情可由当前 DNA 热区打开。
- 2026-07-06：本轮通过。Playwright 截图 `03-desktop-projects-start.png`、`05-desktop-projects-last-key.png` 显示 DNA 已扩展到 11 槽位模型，第 1 与第 10 个项目均在 DNA 上展示；Projects 左侧无“当前 DNA 键位”残留，`result.json` 中 `projectFocusButtons=10`、`projectGoButtons=10`。
- 2026-07-05：本轮通过。最终截图 `03-desktop-projects-zero-slot.png`、`04-desktop-projects-first.png`、`05-desktop-projects-last.png` 显示 0 号聚合空槽、真实项目键位和末端消散段；定点截图 `projects-first.png` 显示 DNA 键只保留 `Key 01`，右侧说明负责完整项目内容，无重复大段信息。
- 2026-07-06：本轮通过。Projects 上端 intro 固定段由 0.16 压缩到 0.025，仅保留短聚合缓冲；截图 `try/visual-qa-20260706-performance/02-desktop-projects-zero-slot.png` 显示 0 号空槽无项目内容且 DNA 已进入视口，`03-desktop-projects-first.png` 显示第 1 项位于 DNA 上，`04-desktop-projects-last.png` 显示第 10 项仍有完整展示段。
- 2026-07-06：本轮通过。Projects HUD 与右侧序号导航改为由本 section 本地滚动进度控制透明度，Data 起点不再残留左侧 `Projects 10/10`；`try/visual-qa-20260706-dna-fade-scatter/result.json` 显示桌面 `dataStart.hudOpacity=0`、`sideOpacity=0`，移动端同为 0。向上回滚复核 `backQuick260` 与 `backSettled` 的 HUD 透明度仍为 0，截图 `07-back-up-260ms.png`、`08-back-up-settled.png` 未出现完整 DNA 先闪现再消失的问题。

## Projects 右侧说明验收

### 验收步骤
1. 在 Projects 段慢速滚动，观察右侧说明区随当前 DNA 键位切换。
2. 点击首个、中间和末尾项目导航点，检查说明区内容是否同步切换。
3. 点击说明区 GitHub 链接，确认外链可打开且不遮挡页面。
4. 从 Projects 继续滚动进入 Data Analysis，检查说明区自然淡出。

### 预期结果
- Projects 不再打开旧项目详情弹层，不恢复 `ProjectVisualBackdrop`、K 线/图表详情背景或遮挡 DNA 的大型背景层。
- 右侧说明区只用文字排版，包含当前项目名称、定位、简介、技术栈、标签和 GitHub 链接。
- 说明区切换时有淡入淡出动效，文字不重叠、不被 DNA 或背景粒子遮挡。
- 桌面与移动视口内说明内容可读，GitHub 链接可点击，切换过程无新增控制台错误。

### 结果记录
- 2026-07-05：通过。桌面详情截图 `03-desktop-flow-detail.png` 展示视觉识别路径图，`05-desktop-chart-detail.png` 展示量化交易 K 线图；移动详情截图 `08-mobile-flow-detail.png` 可读、可关闭、无横向溢出。运行时错误为空，仅有 Three.js 依赖层 `THREE.Clock` 弃用警告。
- 2026-07-05：本轮通过。最终截图 `08-desktop-project-detail.png`、`16-mobile-project-detail.png` 显示项目详情有对应主题背景图形，非纯黑空白；桌面与移动详情均可读、可关闭，`result.json` 中 `projectDetailDesktop=true`、`projectDetailMobile=true`。
- 2026-07-06：本轮通过。Playwright 截图 `05-desktop-project-detail.png` 显示详情层打开成功，背景含项目主题图形，内容可读并保留关闭按钮；控制台错误为空。
- 2026-07-06：本轮通过。项目详情已删除 `ProjectVisualBackdrop`，Playwright `result.json` 中 `projectBackdrop=false`；详情层仅保留半透明轻遮罩，关闭按钮与内容可读。
- 2026-07-06：本轮更新为当前标准。旧项目详情弹层废弃，Projects 以右侧直接说明为主，后续验收不再要求打开/关闭详情层。
- 2026-07-06：本轮通过。Projects 右侧说明随当前 DNA 键位切换，旧项目详情弹层未恢复；截图 `03-desktop-projects-start.png` 与 `05-desktop-projects-last-key.png` 显示右侧说明为纯文字排版并保留 GitHub 链接。
- 2026-07-06：本轮通过。右侧项目说明随末端 DNA 消散同步淡出；`try/visual-qa-20260706-performance/result-filtered.json` 中 `dissolveMetrics.narrativeOpacity=0.481154`、Data 起点 `dataStartMetrics.narrativeOpacity=0`，截图 `05-desktop-projects-dissolve.png` 显示第 10 项说明已淡出且未恢复旧详情弹层。
- 2026-07-06：本轮通过。Data 回滚时不再把 `dnaDissolveProgress` 置回 0，避免上划进入 Projects/Data 边界时先显示完整 DNA；截图 `07-back-up-260ms.png` 与 `08-back-up-settled.png` 显示 Data 详情与背景粒子保持连续，Projects 说明区未残留。
- 2026-07-06：本轮通过。Projects 项目命名与说明已增强技术包装；Lang Drill Agent 右侧说明展示 Demo 与 GitHub 两个入口，Playwright `result.json` 显示桌面 `hasDemoLink=1`。

## Data Analysis 数据分析作品集验收

### 验收步骤
1. 滚动到 `#data-analysis`。
2. 观察页面是否 pin 固定，继续滚轮下滑。
3. 检查柱状图是否随滚轮横向移动，且项目按含金量从左到右排序。
4. 点击至少两根玻璃柱，检查左侧详情是否切换。
5. 检查常规坐标轴、横轴均衡器竖线、方法星云和背景星点是否可见且不遮挡正文。

### 预期结果
- 标题、说明、悬浮详情、常规坐标轴、短彩色竖线波形和 `fluid-glass` 柱体完整可读，详情不得压住主体柱状图。
- 玻璃柱体有不同颜色和真实流体玻璃质感，hover/鼠标移动产生受限 3D 倾斜反馈。
- 柱体高度呈明显正态分布，中间区域高、两端低，并保留局部随机起伏；不得出现大部分柱体近似等高。
- 柱体上不显示含金量数字；排序仍按 `impactScore` 从左到右。
- 滚动进度与点击柱体都能切换详情，详情标题、核心、赛道、主工具和关键发现同步更新；不得显示时间、交付文件数量、方法数量或 `价值点` 旧文案。
- 滚动在该区先推动柱状图横向移动，不能直接跳过整个 section。
- 坐标轴必须包含正常水平轴、左侧纵轴和左侧短横线；水平轴上生成等间距细竖线，竖线高度随滚动变化，当前展示柱体下方形成类似 audio waveform visualization 与 equalizer bars 的局部波峰，不使用旧粒子坐标轴。
- 从 Projects 进入 Data Analysis 后，DNA 必须在柱状图开始横向运动时快速逸散，不能长时间残留在 Data 背景里。
- DNA 消散必须从第 10 个真实项目到第 11 个空槽的中点之后才开始，到达 Data Analysis pinned 起点时完成。
- 方法论图谱改为星云：默认方法名隐藏，当前项目涉及的方法粒子变大并显示方法名；不使用旧卡片 tab 过滤形态。
- Data 首次进入时主内容必须从低透明度淡入到实体状态，文字动画必须在 Data 进入视口后才启动。
- Data 详情标题、概览、核心、赛道、主工具、样本口径和发现点必须静态可读，不使用乱码占位。

### 结果记录
- 2026-07-05：通过。最终截图 `09-desktop-analysis-all.png`、`10-desktop-analysis-finance-tab.png`、`17-mobile-analysis-medical-tab.png` 覆盖桌面全部案例、金融实证过滤与移动医学统计过滤；`result.json` 中 `analysisFilters=true`，方法论图谱与领域分布均存在。
- 2026-07-05：本轮通过。应用内 Browser 验证 Data Analysis 首屏为粒子坐标轴玻璃柱状图；点击第二根柱体后详情切换为“宏观变量与波动率混频建模”；滚轮下滑时页面固定且图表横向平移，后续项目进入视口。
- 2026-07-06：本轮通过。Playwright 截图 `06-desktop-analysis-start.png`、`07-desktop-analysis-scroll.png`、`14-mobile-analysis.png` 显示 `fluid-glass` 柱体、粒子坐标轴、悬浮详情和方法星云；`result.json` 中 `analysisBars=15`、`fluidGlass=21`。
- 2026-07-06：本轮通过。Data Analysis 已改为常规坐标轴 + 620 根等距短彩色竖线波形，左侧纵轴含 180 条短横线，不再使用旧粒子坐标轴；`result.json` 中 `axisBaseline=true`、`axisVertical=true`、`axisParticleDom=0`、`barHeightSpread=356.56`，截图 `06-desktop-data-start.png` 与 `07-desktop-data-scroll.png` 显示柱高呈明显正态分布并随当前柱体形成波峰。
- 2026-07-05：本轮通过。最终截图 `06-desktop-data-start.png`、`07-desktop-data-scroll.png` 与定点截图 `data-start.png` 显示 30 根 `fluid-glass` 柱体、明显正态柱高、正常坐标轴和短彩色竖线波峰；`result.json` 中 `axisParticleDom=0`、`fluidBars=30`、`barHeightSpread=243.812`、`methodNodes=32`。
- 2026-07-06：本轮通过。横轴短竖线从 620 根降为 124 根，x 轴下移至底部附近，y 轴固定在最左侧；`try/visual-qa-20260706-performance/result-filtered.json` 显示 `axisTickCount=124`、`xAxisBottom=841`、`yAxisLeft=35`、`hiddenBarOrbCount=barOrbCount=90`、`barHeightSpread=242.69`。截图 `06-desktop-data-start.png` 与 `07-desktop-data-scroll.png` 显示当前柱 underbar、无柱体中间圆点、波形竖线密度明显降低。
- 2026-07-06：本轮通过。背景层改为 3 个低强度漂浮光斑与 42 个细星点，移除 5 条星座线；Data 波峰改为按当前 `fluid-glass` 柱体实际 DOM 中心测量绑定。`try/visual-qa-20260706-bg-data-timeline/result.json` 显示 `glowCount=3`、`starCount=42`、`constellationCount=0`、`tickCount=124`、三处滚动位置最大 `waveDistance=68.75px`，且 `analysis-detail-swap` 动效存在。截图 `desktop-data-early.png`、`desktop-data-middle.png`、`desktop-data-late.png` 显示波峰位于当前柱下方。
- 2026-07-06：本轮通过。Projects/Data 边界复核通过，Data 起点不再叠加 Projects HUD 或右侧项目导航；`try/visual-qa-20260706-dna-fade-scatter/result.json` 中桌面 `dataStart.projectsTextVisible=false`、移动 `dataStart.projectsTextVisible=false`。
- 2026-07-06：本轮通过。Data 坐标轴改为独立固定在视口坐标系，波峰按滚动进度连续推进，不再读取当前玻璃柱位置造成跳动；`try/visual-qa-20260706-data-transitions/result.json` 显示 `axisLeftStable=true`、`trackMoves=true`、`waveMovesForward=true`，三处 Data 滚动位置中轴线 `axisLeft=34.546875` 保持稳定，图表轨道从 `trackLeft=325.324` 移动到 `-446.931`。背景仍限制 3 个光斑，`glowVisible=true`，截图 `02-data-early.png`、`03-data-middle.png`、`04-data-late.png` 显示青色/玫红/紫色光斑贯穿 Data 视口。
- 2026-07-06：本轮通过。Data 入场触发后移，进入早期主内容低透明度，继续滚动后实体化；Data 详情正文、指标、核心说明和发现点接入进入视口后启动的逐字错峰上滑。`try/visual-qa-20260706-text-glow/result.json` 显示 `dataFadeIn=true`、`dataSolidVisible=true`、`textRevealStarts=true`、`glowCount=5`，截图 `02-data-entering-early.png` 与 `03-data-solid.png` 覆盖从低透明度到实体状态。
- 2026-07-06：本轮通过。Data 概览改为简短作品集文案，详情指标改为核心、赛道、主工具，不再显示方法数量；30 个案例描述已增强交付包装。`try/visual-qa-content-20260706-124502/result.json` 显示桌面/移动 `dataHasNewOverview=true`、`dataHasNoMethodCount=true`、`dataNoGarbled=true`。

## Experience Timeline 与 Contact 验收

### 验收步骤
1. 滚动到 `#experience`，检查太阳系时间线。
2. 悬浮或点击不同星球，检查右侧详情面板是否切换。
3. 滚动到 `#contact`，点击 Email/GitHub 链接，填写表单并提交。

### 预期结果
- 时间线以 R3F 太阳系呈现，一颗星球对应一个时间段，年份已按最新数据校正。
- 星球、轨道、太阳、星点背景和悬浮详情文字可读，首屏不明显裁切关键星球。
- 滚动时相机穿梭星际；点击星球或底部星球导航后，期间、类型、组织、标题、描述和标签同步切换。
- 底部星球导航的年份和标题必须静态可读，不得出现乱码占位或加载失败字符。
- Timeline 场景必须在滚动时固定 pin 于视口，滚轮推进太阳系进度与右侧详情，不得退化为普通页面滑走。
- Contact 信息卡片可读，外链安全打开；表单提交后出现 `消息已发送` toast。
- Contact 必须有明确的大陨石主体与拖尾，核心联系文字写在陨石表面；不能退化为纯黑背景上的普通大字排版。
- 页面底部 footer 与正文不重叠，不被 3D Canvas 或固定导航遮挡。
- Email 卡片不得把邮箱拆成 `c / om` 这类无意义换行。
- Timeline 与 Contact 首次进入视口时内容必须明显淡入；Timeline 详情、标签和底部导航必须静态可读，Contact 联系信息和表单文字可保留逐字错峰上滑入场。

### 结果记录
- 2026-07-05：通过。最终截图 `11-desktop-experience.png`、`12-desktop-contact-toast.png`、`18-mobile-contact.png` 覆盖时间线、联系表单与移动联系信息；`result.json` 中 `timelineCards=true`、`contactToast=true`，无横向溢出。
- 2026-07-05：本轮通过。应用内 Browser 验证 Timeline 已替换为太阳系，年份显示为 2023、2025、2026、2026；Contact Email 跨列显示完整，表单填写测试数据后出现 `消息已发送` toast。
- 2026-07-06：本轮通过。Playwright 截图 `08-desktop-timeline-start.png`、`09-desktop-timeline-travel.png` 显示 R3F 太阳系和相机穿梭状态，无方形光效遮罩；`10-desktop-contact.png`、`11-desktop-contact-toast.png` 显示 Contact 表单 toast，`result.json` 中 `contactToast=true`。
- 2026-07-06：本轮通过。Timeline 恢复固定滚动效果，`result.json` 中 `timelinePin=true`、`timelinePinnedAtMid=true`；截图 `08-desktop-timeline.png` 显示太阳系固定在视口中，右侧详情随进度切换。
- 2026-07-05：本轮通过。最终截图 `09-desktop-timeline-mid.png` 与定点截图 `timeline-mid.png` 显示 Timeline 中段固定在视口内、星球与年份正对用户；`10-desktop-contact.png` 显示 Contact 陨石主体与拖尾可见。
- 2026-07-06：本轮通过。Timeline 相机改用星球自身轨道公式聚焦激活星球，截图 `try/visual-qa-20260706-performance/08-desktop-timeline-mid.png` 显示相机对准当前 2025 星球且年份正对用户；`result-filtered.json` 显示 `timelinePin=true`。Contact 截图 `09-desktop-contact.png` 与 `13-mobile-contact.png` 显示陨石主体和文字仍可见。
- 2026-07-06：本轮通过。Timeline 详情块加入整块切换动效，背景从额外径向光斑改为弱线性流动渐变，避免与统一 Cosmic 背景叠加过亮。`try/visual-qa-20260706-bg-data-timeline/result.json` 显示 `timeline.detailAnimation=detail-swap-in`、`pinTop=0`、`pinHeight=900`，截图 `desktop-timeline.png` 显示太阳系固定在视口内且详情文字完整。
- 2026-07-06：本轮通过。Timeline 与 Contact 的 section 内容加入滚动绑定淡入；`try/visual-qa-20260706-data-transitions/result.json` 显示 `timelineFadeIn=true`、`contactFadeIn=true`，Timeline 视口内内容透明度从 `0.7913` 提升到 `1`，Contact 视口内内容透明度从 `0.7159` 提升到 `0.8662`。截图 `05-timeline-fade.png`、`06-contact-fade.png` 显示从 Data 到 Timeline、Timeline 到 Contact 的下一个板块内容逐步实体化。
- 2026-07-06：本轮通过。Timeline 与 Contact 淡入触发点后移，进入早期保持低透明度，靠近主体后实体化；Timeline 标题、详情、标签、底部导航，以及 Contact 陨石文字、联系信息、表单标签和 footer 均接入逐字错峰上滑。`try/visual-qa-20260706-text-glow/result.json` 显示 `timelineFadeIn=true`、`contactFadeIn=true`、`textRevealStarts=true`，截图 `04-timeline-entering.png`、`05-timeline-solid.png`、`06-contact-entering.png`、`07-contact-solid.png` 覆盖过渡过程。
- 2026-07-06：本轮通过。Timeline 数据分析与 AI Agent 工作流时间均更新为 `2025.05-至今`，新增 `2025.12-至今` 闲鱼商业化运营实验星球；详情与底部星球导航改为静态可读。`try/visual-qa-content-20260706-124502/result.json` 显示桌面/移动 `timelineHasXianyu=true`、`timelineNavReadable=true`、`timelineNoGarbled=true`。

## SEO、导航与构建验收

### 验收步骤
1. 运行 `npm run lint`。
2. 运行 `npm run build`。
3. 检查 `<html lang>`、页面 title、description、keywords、Open Graph 元数据。
4. 滚动页面并点击导航中的 About、Projects、Data Analysis、Experience、Contact。

### 预期结果
- lint 与生产构建零错误。
- 页面元数据覆盖 FAN JUN JIE、AI 产品、数据分析、统计建模、Three.js 等关键词。
- 导航滚动后毛玻璃背景出现，当前 section 高亮准确，点击链接平滑滚动到目标 section。

### 结果记录
- 2026-07-05：通过。`npm run lint`、`npm run build` 均零错误；`result.json` 中 `metadataPresent=true`、`allSectionsExist=true`、`runtimeErrorsEmpty=true`，导航点击 Data Analysis 后目标 section 正常进入视口。
- 2026-07-06：本轮通过。`npm run lint`、`npm run build` 均零错误；`result.json` 中 title 为 `FAN JUN JIE | AI Product & Data Analysis Portfolio`，`navHasRoundedFrame=false`，导航当前项以颜色和下划线显示。
- 2026-07-06：本轮通过。`npm run lint` 与 `npm run build` 均零错误；`try/visual-qa-20260706-dna-data-contact/result.json` 中页面 title 正确、所有 section 存在、无 banned text。
- 2026-07-05：本轮通过。`npm run lint` 与 `npm run build` 均零错误；最终 `result.json` 中 title 正确、所有 section 存在、无“当前 DNA”、无旧 `ProjectVisualBackdrop`、无旧粒子坐标轴 DOM。
- 2026-07-06：本轮通过。`npm run lint` 与 `npm run build` 均零错误；`try/visual-qa-20260706-performance/result-filtered.json` 中 title 正确、无“当前 DNA”、无旧 `ProjectVisualBackdrop`、无横向溢出。
- 2026-07-06：本轮通过。`npm run lint` 与 `npm run build` 均零错误；`try/visual-qa-20260706-bg-data-timeline/result.json` 中 `consoleErrors=[]`，只记录 Three.js 依赖弃用 warning 与 Playwright 截图触发的 WebGL `ReadPixels` 性能 warning。
- 2026-07-06：本轮通过。`npm run lint` 与 `npm run build` 均零错误；`try/visual-qa-20260706-dna-fade-scatter/result.json` 中 `consoleErrors=[]`，仅有 Three.js 依赖弃用 warning 与 Playwright 截图触发的 WebGL `ReadPixels` 性能 warning。
- 2026-07-06：本轮通过。`npm run lint` 与 `npm run build` 均零错误；`try/visual-qa-20260706-data-transitions/result.json` 中 `consoleErrors=[]`，仅有 Three.js 依赖弃用 warning 与 Playwright 截图触发的 WebGL `ReadPixels` 性能 warning。
- 2026-07-06：本轮通过。`npm run lint` 与 `npm run build` 均零错误；`try/visual-qa-20260706-text-glow/result.json` 中 `consoleErrors=[]`，仅有 Three.js 依赖弃用 warning 与 Playwright 截图触发的 WebGL `ReadPixels` 性能 warning。
- 2026-07-06：本轮通过。`npm run lint` 与 `npm run build` 均零错误；`try/visual-qa-content-20260706-124502/result.json` 中桌面/移动 `consoleErrors=[]`、`bannedFound=[]`、`bannedRegexFound=[]`，无旧解说文案、旧时间和旧数值残留。

## 响应式验收

### 验收步骤
1. 使用移动视口打开首页。
2. 从 Hero 滚动到 Projects 段。
3. 检查 Projects 右侧说明、Data 柱状图和 Contact 陨石信息。
4. 检查导航、标题、项目标签、说明文字与表单按钮。

### 预期结果
- 页面主要文字不溢出容器，不遮挡按钮和 3D 主体。
- Projects 状态栏、项目标签、右侧说明和导航在移动端仍可读可点。
- Data 柱状图、方法星云和 Contact 陨石文字在移动端不产生横向溢出。

### 结果记录
- 2026-07-05：通过。移动视口 390x844 下 Projects 段、项目详情打开与关闭均通过；检测结果 `noHorizontalOverflow=true`、`mobileDetailThemeSvg=true`。
- 2026-07-05：本轮通过。移动视口 390x844 覆盖 Hero、Projects、项目详情、Data Analysis、Contact；最终检测 `mobileNoHorizontalOverflow=true`、`projectDetailMobile=true`，截图位于 `try/visual-qa-20260705-222843/`。
- 2026-07-06：本轮通过。移动视口 390x844 覆盖 Hero、Projects、Data Analysis、Contact；`result.json` 中 `horizontalOverflow=false`，截图 `13-mobile-projects.png` 未再出现 About 标签溢入 Projects。
- 2026-07-06：本轮通过。移动视口 390x844 覆盖 Hero、Projects、Data Analysis、Contact；`result.json` 中 `mobile.horizontalOverflow=false`、`mobile.dataBars=22`、`mobile.contactTextVisible=true`。
- 2026-07-05：本轮通过。移动视口 390x844 覆盖 Hero、Projects、Data Analysis、Contact；最终 `result.json` 中 `mobile.horizontalOverflow=false`、`mobile.dataBars=30`、`mobile.contactTextVisible=true`、`mobile.nextPortalHidden=true`。
- 2026-07-06：本轮通过。移动视口 390x844 覆盖 Hero、Projects、Data Analysis、Contact；`try/visual-qa-20260706-performance/result-filtered.json` 中 `mobile.horizontalOverflow=false`、`mobile.dataBars=30`、`mobile.contactMeteor=true`。移动端 Data 详情仍较密集，作为后续视觉风险记录。
- 2026-07-06：本轮通过。移动视口 390x844 复核 Data Analysis 响应式详情，已缩小移动端标题、正文和指标尺寸并隐藏过密发现列表；`try/visual-qa-20260706-bg-data-timeline/result.json` 显示 `mobile.layout.horizontalOverflow=false`、`titleBottom=313.59`、`chartTop=492.03`、`detailAnimation=detail-swap-in`。
- 2026-07-06：本轮通过。移动视口 390x844 复核 Data 起点，`try/visual-qa-20260706-dna-fade-scatter/result.json` 显示 `mobile.dataStart.bodyOverflowX=0`、`hudOpacity=0`、`sideOpacity=0`，无 Projects 残留遮挡。
- 2026-07-06：本轮通过。移动视口 390x844 复核 Data 光斑、坐标轴和横向溢出；`try/visual-qa-20260706-data-transitions/result.json` 显示 `mobile.horizontalOverflow=0`、`glowCount=3`、`glowOpacity=[0.774519,0.801006,0.826374]`，移动端无横向溢出。
- 2026-07-06：本轮通过。移动视口 390x844 复核增强后的 5 个光晕与 Data 入场文字状态；`try/visual-qa-20260706-text-glow/result.json` 显示 `mobileData.bodyOverflowX=0`、`mobileData.glowCount=5`、`noOverflow=true`，截图 `08-mobile-data-glows.png` 无横向溢出。
- 2026-07-06：本轮通过。移动视口 390x844 复核 About、Projects、Data、Timeline；`try/visual-qa-content-20260706-124502/result.json` 显示 `mobile.horizontalOverflow=false`、`mobile.dataNoGarbled=true`、`mobile.timelineNoGarbled=true`、`mobile.timelineNavReadable=true`。
