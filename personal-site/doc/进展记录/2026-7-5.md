# 2026-7-5 进展记录

## 2026-07-05 21:05 ~ 2026-07-05 21:17

本阶段完成内容：读取项目规则、React/Next/R3F/动画相关技能说明；确认 Git 仓库根目录为 `D:\0文件夹\个人站`，前端项目在 `D:\0文件夹\个人站\personal-site`；当前分支 `trae/feature-starry-personal-site` 工作区干净，并创建变更前检查点提交 `819390b`。启动本地服务 `http://localhost:3000`，完成浏览器基线视觉检查，确认项目段存在 Hero 粒子干扰 DNA、项目文字与 DNA 重叠、详情层黑色背景缺少项目主题视觉、项目切换以离散索引跳动导致滚动节奏过快等问题。

新增/修改/生成的文件清单与用途说明：`try/next-dev.log`、`try/next-dev.err.log` 为本地开发服务调试日志；`doc/进展记录/2026-7-5.md` 为本轮进展记录。

高风险标记与回退方案：下一阶段会修改 3D 场景状态、DNA 标签定位、项目详情背景与验收规则，属于多文件视觉交互修改。若出现渲染失败或交互退化，可回退到变更前检查点 `819390b`。

错误汇报：应用内 Browser 的 DOM 快照接口报错 `incrementalAriaSnapshot is not a function`，已改用截图、控制台日志和只读页面评估继续做视觉验收。

## 2026-07-05 21:17 ~ 2026-07-05 21:52

本阶段完成内容：修复 Projects 段 DNA 浏览体验，将 Hero 粒子与 DNA 项目段分离；新增连续 `projectProgress` 驱动 DNA 相机，滚轮小步滚动按顺序从项目 1 渐进到项目 2、项目 3；降低 DNA 后景粒子密度；把项目标签改为固定在 DNA 附近的 DOM 按钮；详情页从纯黑背景改为按项目类型渲染主题图形，覆盖视觉识别路径、数据主线图、波形、节点、棋盘与量化 K 线图；移动端压缩左侧状态栏，消除项目文字与 DNA 标签重叠。同步更新项目长期规则与严格浏览器视觉验收标准。运行 `npm run lint` 与 `npm run build` 均通过。

新增/修改/生成的文件清单与用途说明：`.gitignore` 增加 `/try/`，避免调试文件进入提交；`AGENTS.md` 更新项目目标、目录职责、读取顺序、运行命令、严格浏览器验收规则和安全约束；`doc/验收标准.md` 新增并记录本轮验收结果；`doc/进展记录/2026-7-5.md` 记录本轮阶段进展；`src/components/sections/ProjectsSection.tsx` 调整滚动进度、项目标签和移动端状态栏；`src/components/three/SceneContext.tsx` 增加项目滚动进度状态；`src/components/three/Scene.tsx` 分离 Hero 粒子、DNA 场景与项目主题场景；`src/components/three/ParticleField.tsx`、`src/components/three/shaders/particles.frag.ts` 增加粒子透明度控制；`src/components/three/dna/DNAHelixScene.tsx`、`src/components/three/dna/dnaGeometry.ts`、`src/components/three/dna/shaders/dna-helix.vert.ts`、`src/components/three/dna/shaders/dna-helix.frag.ts` 优化 DNA 可见性、粒子密度、相机和淡入淡出；`src/components/three/ProjectScene.tsx` 重做项目 3D 主题视觉；`src/components/ui/ProjectVisualBackdrop.tsx` 新增项目详情 SVG 主题背景；`src/components/ui/ProjectDetail.tsx` 接入主题背景并改造详情遮罩；`src/components/ui/StatCounter.tsx` 修正 effect 依赖；`eslint.config.mjs` 为 R3F/Three 可变对象补充 ESLint 规则例外。调试与验收文件生成于 `try/next-dev.log`、`try/next-dev.err.log`、`try/visual-qa-20260705-214900/`、`try/visual-qa-20260705-215203/`，均为可删除调试文件且已被忽略。

错误汇报：应用内 Browser 能列出本地标签页，但两次读取页面状态时超时并重置；已记录为工具链异常，改用 Edge Chromium + Playwright 完成同等浏览器视觉验收。第一次自动化验收的详情检测选择器误选导航层，且把开发环境隐藏 portal 误判为框架错误覆盖层；已修正检测口径并复验通过。移动端初次截图发现左侧项目说明与 DNA 标签轻微重叠；原因是移动端状态栏仍显示项目名和副标题，解决方案为移动端仅保留 Projects 标题和序号，项目名固定由 DNA 标签展示。

验收结果：`try/visual-qa-20260705-215203/result.json` 显示 `noVisibleFrameworkOverlay=true`、`canvasPresent=true`、`noHorizontalOverflow=true`、`flowDetailThemeSvg=true`、`chartDetailThemeSvg=true`、`mobileDetailThemeSvg=true`、`returnAboveProjects=true`、`runtimeErrors=[]`。仅保留 Three.js 依赖层 `THREE.Clock` 弃用警告，不影响本轮功能。

回退方案：如后续发现视觉回归，可先回退本轮功能提交；若需回到修改前状态，可回退检查点提交 `819390b` 之后的改动。无工作区外文件写入。
