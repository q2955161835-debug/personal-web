# 2026-7-5 进展记录（个人站）

## 2026-07-05 14:00 ~ 2026-07-05 14:30

### 本阶段完成内容
- 在 `d:\0文件夹\个人站` 初始化 Git 仓库，配置用户名邮箱。
- 与用户确认关键决策：工作目录 `d:\0文件夹\个人站`、原生 Three.js + GSAP 技术栈、8 主行星 + 星环 90 星体规模、Three.js Shape + SVG Logo 实现方案。
- 创建 Vite 6 + TypeScript 5.7 + Three.js 0.170 + GSAP 3.13 + Lenis + Tailwind CSS 3.4 项目骨架。
- 实现阶段1 基础场景：WebGL2 渲染器（ACES 色调映射 + SRGB 输出）、PerspectiveCamera、三层星空背景（远景 5000 + 中景 1500 + 近景 300 闪烁星）、鼠标视差。
- 创建 8 个主行星项目数据 `src/data/projects.ts`，含 Lang Drill Agent、异环麻将、AI 五子棋、codex 视频、GARCH-MIDAS、烟台海洋、河北生态、HPLC 灰色的完整简介、含金量、技术栈、详情和链接。
- 创建项目长期规则 `AGENTS.md`、`doc/验收标准.md`、`.env.example`、`.gitignore`。

### 新增/修改/生成文件清单与用途说明
- `package.json`：项目依赖与脚本（dev/build/preview/typecheck）。
- `vite.config.ts`：Vite 配置，含路径别名 @/@shaders/@components/@data/@utils。
- `tsconfig.json`：TypeScript 严格模式配置，含路径映射。
- `tailwind.config.js`：Tailwind 主题，含星空色板（space/nebula/star）与动画（twinkle/float/glow）。
- `postcss.config.js`：PostCSS 配置（tailwind + autoprefixer）。
- `index.html`：HTML 入口，含 loader、#webgl canvas、#ui-overlay、#app。
- `src/main.ts`：应用入口，启动 SceneManager、隐藏 loader、注入顶部导航与 HUD、WebGL 不支持降级提示。
- `src/style.css`：全局样式，含玻璃面板、模态框、HUD、滚动提示、流星 canvas、reduced motion。
- `src/scene/Renderer.ts`：WebGL2 渲染器封装，ACESFilmicToneMapping + SRGBColorSpace + 高性能偏好。
- `src/scene/SceneManager.ts`：场景管理器，scene/camera/clock/RAF 循环 + 鼠标视差 + scrollProgress 预留接口。
- `src/components/Starfield.ts`：三层星空 Points（远/中/近），AdditiveBlending + sizeAttenuation + 闪烁动画。
- `src/data/projects.ts`：8 个主行星项目数据（ProjectPlanetData 接口 + PROJECT_PLANETS 数组）。
- `AGENTS.md`：项目长期规则、目录职责、10 阶段实施计划、资源引用、安全约束。
- `doc/验收标准.md`：验收项 A（项目骨架与本地启动）+ B-J 待定义。
- `doc/进展记录/2026-7-5.md`：本文件。
- `.env.example`：环境变量假账本（GitHub/Lang Drill/联系方式）。
- `.gitignore`：忽略 node_modules/dist/.env/try 等。

### 工作区外读取与引用
- `d:\0文件夹\简历+个人站\AGENTS.md`：读取简历项目长期规则，了解简历已完成、个人站为新任务。
- `d:\0文件夹\简历+个人站\doc\进展记录\2026-7-5.md`：读取简历项目当日进展，确认简历任务已完成。
- `d:\0文件夹\简历+个人站\web\index.html`：读取 activetheory 复刻状态，确认 3D 渲染未成功，决定从零用原生 Three.js。
- `d:\1Folder\数据分析工作区\AGENTS.md`：读取数据分析工作区规则，确认 93 个项目位置。
- `d:\1Folder\数据分析工作区\doc\任务索引.md`：读取任务索引，确认归档库 78 个项目 + 15 个标准项目。
- `d:\1Folder\数据分析工作区\doc\整理清单\整理结果_2026-06-29.md`：读取整理结果，确认 78 项目 619 文件。
- 通过 search agent 读取简历源文本（`d:\0文件夹\简历+个人站\scripts\build_resume.py`）和数据分析作品集（`d:\1Folder\数据分析工作区\tasks\20260702-数据分析作品集\output\数据分析作品集.md`），提取 8 个主行星项目数据。

### 错误汇报
- 无新增错误。

### 验证结果
- 待运行 `npm install` 和 `npm run dev` 后验证验收项 A。

## 下一步
- 安装依赖，启动开发服务器，验证验收项 A（项目骨架与本地启动）。
- 验证通过后提交 Git 检查点，进入阶段2（入口恒星首页）。

## 2026-07-05 14:30 ~ 2026-07-05 15:10

### 本阶段完成内容
- 实现阶段2 入口恒星首页，构建首页核心视觉与交互层。
- 编写太阳表面湍流 shader：3D Simplex Noise 顶点位移 + FBM 多层噪声片元 + Fresnel 边缘辉光 + 黑子分层；颜色配置 core 0xfff4cc / mid 0xff9d4a / edge 0xff4a2a。
- 编写日冕 shader：BackSide + AdditiveBlending + Fresnel 脉动，配置 1.35x 日冕 + 1.8x 远场辉光三重球体。
- 实现入口恒星组件 `HomeStar.ts`：IcosahedronGeometry(8, 64) 内核 + 日冕 + 辉光，update 同步 uTime uniform 与缓慢自转。
- 实现倒V Logo 组件 `InvertedVLogo.ts`：两条 ExtrudeGeometry 楔形臂（左 dir=-1，右 dir=1），顶端间隙 0.8 单位（顶内 0.4，顶外 0.95），MeshStandardMaterial 紫蓝双色（左 0x9d6bff，右 0x4a9eff）+ LineBasicMaterial 描边，update 含轻微悬浮旋转与 emissiveIntensity 脉动。
- 实现鼠标流星组件 `MeteorField.ts`：独立 2D canvas（mix-blend-mode: screen），鼠标移动生成 MeteorParticle（hue 200-320 蓝-紫-粉），含 trail 拖尾（最多14点），destination-out 渐隐 + lighter 混合 + radialGradient 头部光点，限流 180 粒子。
- 集成 `SceneManager.ts`：imports 添加 HomeStar 和 InvertedVLogo，constructor 实例化 homeStar(0,0,-15) 和 logo(0,0.5,8)，相机 z=38，灯光改为 ambient + sunLight(PointLight 0xffd966) + fillLight(DirectionalLight 0x9d6bff)，animate 调用 homeStar.update 和 logo.update，dispose 释放资源。
- 更新 `main.ts`：实例化 MeteorField 并 start，注入 Hero 姓名/求职方向 UI（FAN JUNJIE / 范俊杰 / AI 产品 / 数据分析实习 / 苏州 · 盐城师范学院 · GitHub 主页），HUD SCENE 改为 HOME_STAR。
- 更新 `style.css`：新增 .hero-text 样式（左下角定位，渐变文字 -webkit-background-clip，clamp 响应式字号，hero-fade-in 动画 1.4s）。

### 新增/修改/生成文件清单与用途说明
- `src/shaders/sun.ts`：太阳 Simplex Noise 湍流 shader + 日冕 Fresnel shader（导出 4 个 shader 字符串）。
- `src/components/HomeStar.ts`：入口恒星组件，三重球体（内核 + 日冕 + 辉光）+ update(elapsed, delta) + dispose。
- `src/components/InvertedVLogo.ts`：倒V Logo 组件，ExtrudeGeometry 双臂 + 描边 + update + dispose。
- `src/components/MeteorField.ts`：鼠标流星 2D canvas 组件，含 MeteorParticle 接口、spawn、loop、dispose。
- `src/scene/SceneManager.ts`：修改 - 集成 HomeStar 和 InvertedVLogo，更新 animate 和 dispose。
- `src/main.ts`：修改 - 实例化 MeteorField，注入 Hero UI，更新清理逻辑。
- `src/style.css`：修改 - 新增 .hero-text 系列样式。

### 工作区外读取与引用
- 无新增。

### 错误汇报
- 启动 dev 服务器时端口 5173 被占用（前次会话遗留的 dev 服务器仍在运行）。改用 `npm run build` 验证生产构建成功，dist 产物正常生成（three 504kB / gzip 128kB，index 20.5kB / gzip 6.7kB）。

### 验证结果
- `npm run typecheck` 退出码 0，无类型错误。
- `npm run build` 构建成功，13 模块转换通过，无警告。
- 阶段2 Git 检查点提交：`9b14005 feat: 阶段2 入口恒星首页 - 太阳shader+倒VLogo+鼠标流星+姓名UI`。

## 下一步
- 用户在浏览器人工验收验收项 B（太阳 shader、倒V Logo、姓名 UI、鼠标流星的视觉效果）。
- 验收通过后进入阶段3（滚动叙事系统）：GSAP ScrollTrigger + Lenis 平滑滚动 + 相机贝塞尔曲线 + 行星定位。

## 2026-07-05 15:10 ~ 2026-07-05 16:40

### 本阶段完成内容
- 阶段2 入口恒星首页经过多轮用户反馈迭代完成。
- 太阳 shader 大幅增强：六阶 FBM + 多尺度噪声（颗粒/超颗粒/大尺度）+ 黑子本影半影 + 光斑 + 临边昏暗 + 日珥位移 + 溅射喷发。
- 日冕经过 5 轮迭代：从规整球壳 → 多尺度噪声扭曲 → 双层 Fresnel 柔和扩散 → 随机突起（小部分明显）。
- 鼠标效果经过 4 轮迭代：爆裂式流星 → activetheory 风格光球拖尾 → 彗星头尾物理分离 → 全冷色调融入星空。
- 倒V Logo 经用户要求移除（先去掉）。
- 首页 Hero UI 精简：仅保留 FAN JUNJIE 英文大字，删除中文、求职方向、苏州、学校、GitHub 链接。

### 关键决策与迭代历程
1. 第一版：高科技抽象星（晶核+线框+轨道环），用户反馈"不如之前的太阳"。
2. 第二版：恢复太阳方向，增强细节（六阶 FBM + 黑子 + 光斑 + 临边昏暗 + 日珥），用户反馈"太刺眼，日冕太规整"。
3. 第三版：太阳降亮（1.6→1.05），日冕多尺度噪声扭曲去规整，鼠标改 activetheory 风格。
4. 第四版：日冕双层 Fresnel 柔和化，彗星头尾物理分离，删除折射层（用户反馈效果太差）。
5. 第五版：日冕加入随机突起（小部分明显），彗星全冷色调融入星空。

### 新增/修改/生成文件清单与用途说明
- `src/shaders/sun.ts`：增强版太阳 shader（sunVertex + sunFragment + coronaVertex + coronaFragment），含 Simplex Noise、FBM、黑子、光斑、临边昏暗、日珥、溅射、随机突起。
- `src/components/HomeStar.ts`：入口恒星组件，四层结构（内核 IcosahedronGeometry(96) + 内日冕 1.25x + 外日冕 1.55x + 远场辉光 1.95x）。
- `src/components/MeteorField.ts`：鼠标彗星头尾分离版，全冷色调（蓝-青-紫），TrailParticle 独立消散。
- `src/scene/SceneManager.ts`：修改 - 移除 InvertedVLogo，灯光恢复暖色调（ambient 0x2a1a1a + sunLight 0xffb060 + fillLight 0x9d6bff）。
- `src/main.ts`：修改 - Hero UI 精简为仅 FAN JUNJIE。
- `src/style.css`：修改 - .hero-text 样式调整。
- 删除：`src/components/InvertedVLogo.ts`（用户要求去掉 Logo）。

### 工作区外读取与引用
- 无新增。

### 错误汇报
- 无错误。typecheck 和 build 全程通过。

### 验证结果
- `npm run typecheck` 退出码 0，无类型错误。
- `npm run build` 构建成功（three 470kB / gzip 119kB，index 29kB / gzip 8kB）。
- 阶段2 迭代 Git 检查点：
  - `1c1e00c` 太阳重做增强细节
  - `63e299b` 太阳溅射+日冕去规整+彗星+UI精简
  - `8cd348a` 太阳降亮+日冕淡化+折射层（后删除）
  - `4a6ff12` 彗星头尾分离+日冕柔和双层Fresnel+删除折射层
  - `ff5deea` 日冕随机突起+彗星全冷色调融入星空

## 下一步
- 阶段2 入口恒星首页完成，进入阶段3 滚动叙事系统。
- 阶段3：Lenis 平滑滚动 + GSAP ScrollTrigger + 相机贝塞尔曲线路径 + 8 主行星沿路径定位 + Hero 文字滚动隐藏。

## 2026-07-05 17:00 ~ 2026-07-05 23:30

### 本阶段完成内容
- 阶段3 滚动叙事系统：Lenis 平滑滚动 + GSAP ScrollTrigger scrub + CatmullRomCurve3（11 个关键点）相机路径 + `window.__scrollProgress` 桥接 main.ts。
- 阶段4-5 8 主行星自定义表面 shader：word-galaxy / yolo-tiles / gomoku-grid / video-frames / time-series / multi-model / map-grid / chromatogram，共享 Simplex Noise + FBM，每种 surfaceType 独特 vertex/fragment shader。
- 行星定位：沿路径 0.10-0.80 进度分布，左右交替偏移 ±6 单位，使用路径切线计算侧向偏移。
- 阶段6 数据分析星环：中心恒星（Fresnel 工作流脉动）+ 8 领域分组（92 小星体），每组独立半径环线，正反交替自转。
- 阶段7 转场动效：InstancedMesh 300 陨石 + 800 粒子星云 + 3 颗 CatmullRomCurve3 彗星 + 200 点星团。
- 阶段8 技能证书空间站：TorusGeometry 中央环 + 6 BoxGeometry 模块舱 + 6 beacon 闪烁 + 4 太阳能板 + 3 通信天线，纯程序化建模无外部模型依赖。
- 阶段8 UI：技能矩阵 6 类（编程/数据/AI/产品/语言/证书）+ 联系方式 4 项，滚动 85% 后淡入。
- 阶段9 玻璃面板与详情交互：Raycaster 检测行星悬停/点击，模态框含简介/含金量/详情/技术栈/特性/成果/链接，玻璃 backdrop-blur，支持 ESC / backdrop / close 按钮关闭。

### 新增/修改/生成文件清单与用途说明
- `src/scene/ScrollController.ts`：滚动控制器，Lenis + ScrollTrigger + CatmullRomCurve3 路径 + getPathPoint/getPathTangent。
- `src/shaders/planet-surfaces.ts`：8 种行星表面 shader 集合 + getPlanetShader 选择器。
- `src/components/PlanetPlaceholder.ts`：行星组件，IcosahedronGeometry(32) + 表面 shader + Fresnel 大气，mesh 改为 public 供 raycasting。
- `src/components/DataAnalysisRing.ts`：数据分析星环，中心恒星 + 8 组 92 星体，性能优化版（共享 geometry + material）。
- `src/components/TransitionEffects.ts`：转场动效，InstancedMesh 陨石 + 星云 + 彗星 + 星团。
- `src/components/SkillStation.ts`：技能空间站，程序化建模组合体。
- `src/data/skills.ts`：技能矩阵 + 联系方式数据。
- `src/scene/PlanetInteraction.ts`：Raycaster 行星交互管理器，registerPlanet + update + setEnabled。
- `src/scene/SceneManager.ts`：集成所有组件，添加 setModalOpenCallback / setInteractionEnabled。
- `src/main.ts`：UI 注入 + 滚动监听 + openProjectModal 玻璃模态框 + setModalOpenCallback 注册。
- `src/style.css`：新增 .skill-station-ui / .project-modal 系列样式。

### 错误汇报
- SceneManager setupPlanets 重复调用：Edit 操作导致 constructor 中 setupPlanets() 被调用两次。修复：用 Write 完整重写 SceneManager.ts，确保 scrollController 在 setupPlanets 和 setupDataAnalysisRing 之前创建。
- main.ts Edit 字符串未找到：old_string 与文件实际内容不匹配。修复：改用 Write 完整覆盖 main.ts。

### 验证结果
- `npm run typecheck` 退出码 0，无类型错误。
- `npm run build` 构建成功（27 模块，three 489kB / gzip 124kB，index 132kB / gzip 43kB）。
- 阶段9 Git 检查点：`5724cef feat: 阶段9 玻璃面板与详情交互 - Raycasting+模态框`。

## 2026-07-05 23:30 ~ 2026-07-06 00:30

### 本阶段完成内容（阶段10 性能优化与验收）
- DataAnalysisRing 性能优化：92 个小星体 IcosahedronGeometry detail 从 4 降为 1（小尺寸看不出细节，减少 75% 三角形），改为共享 unit geometry + scale 区分尺寸，每组共享 material（8 套而非 92 套）。
- 移除每帧 92 个 mesh opacity 闪烁循环（CPU 瓶颈），改为静态 opacity 0.85，动态感由中心恒星脉冲 + 各组旋转承担。
- dispose 改为去重释放（Set 追踪已释放 material/geometry），避免重复释放共享资源。
- SceneManager 添加 prefers-reduced-motion 检测：matchMedia 检测，若启用则 animScale=0.3 降低动画速度。
- SceneManager 添加 visibilitychange 监听：页面隐藏时暂停渲染循环（this.hidden = true 时停止 RAF），恢复可见时自动重启。
- main.ts handleScroll 添加 setInteractionEnabled(progress < 0.88)：滚动到空间站区域禁用行星交互避免误触。
- 更新验收标准文档 C-J 项。

### 新增/修改/生成文件清单与用途说明
- `src/components/DataAnalysisRing.ts`：修改 - 共享 geometry/material + 移除每帧 opacity 循环 + dispose 去重。
- `src/scene/SceneManager.ts`：修改 - 添加 reducedMotion/animScale/hidden 字段 + onVisibilityChange + animate 中应用 animScale + dispose 移除监听。
- `src/main.ts`：修改 - handleScroll 中调用 setInteractionEnabled。
- `doc/验收标准.md`：修改 - 补充 C-J 验收项步骤与预期结果。

### 错误汇报
- 无错误。typecheck 和 build 全程通过。

### 验证结果
- `npm run typecheck` 退出码 0。
- `npm run build` 构建成功（27 模块，three 489kB / gzip 124kB，index 132kB / gzip 43kB）。
- 性能优化点：
  - InstancedMesh 陨石带（300 实例 1 drawcall）
  - DataAnalysisRing 共享 geometry/material（92 mesh 8 drawcall 而非 92）
  - 像素比限制为 min(devicePixelRatio, 2)
  - visibilitychange 暂停隐藏渲染
  - prefers-reduced-motion 支持

## 下一步
- 用户在浏览器人工验收验收项 C-J（滚动叙事、8 行星、星环、转场、空间站、模态框、性能 60fps）。
- 验收通过后合并 main 分支并清理 trae/feature-starry-personal-site 分支。
