# 验收标准

## 测试环境
- 操作系统：Windows 11，本地 PowerShell。
- Node.js：v22.17.1，npm 10.9.2。
- 浏览器：Chrome / Edge / Firefox / Safari 最新版（需支持 WebGL2）。
- 工作区：`d:\0文件夹\个人站`。

## 验收项 A：项目骨架与本地启动
- 验收步骤：
  1. 运行 `npm install` 安装依赖。
  2. 运行 `npm run dev` 启动开发服务器。
  3. 用 Edge 或 Chrome 打开 `http://127.0.0.1:5173/`。
  4. 等待 1 秒后 loader 隐藏，看到星空背景。
  5. 运行 `npm run typecheck` 确认无类型错误。
- 预期结果：
  - `node_modules/` 安装成功，无致命错误。
  - 开发服务器在 127.0.0.1:5173 启动。
  - 页面加载后 loader（橙色脉冲圆）在 1 秒内淡出。
  - 全屏黑色背景上出现三层星空（远景密集小白点 + 中景彩色星点 + 近景闪烁亮星）。
  - 鼠标移动时相机有轻微视差。
  - 顶部导航显示 "FAN JUNJIE · 范俊杰" 和 GitHub / Contact 链接。
  - 底部显示滚动提示 "SCROLL TO EXPLORE" 和 HUD 信息。
  - `npm run typecheck` 退出码 0。
- 结果记录：
  - 2026-07-05：待验证。

## 验收项 B：入口恒星首页（阶段2）
- 待定义：太阳表面湍流 shader、倒 V Logo 几何体、姓名求职方向文字、鼠标流星效果。

## 验收项 C：滚动叙事系统（阶段3）
- 待定义：GSAP ScrollTrigger + Lenis 平滑滚动、相机贝塞尔曲线路径、行星定位。

## 验收项 D：4个核心项目行星（阶段4）
- 待定义：Lang Drill Agent / 异环麻将 / AI 五子棋 / codex 视频，每个行星表面场景建模 + 大气粒子。

## 验收项 E：4个数据分析代表行星（阶段5）
- 待定义：GARCH-MIDAS / 烟台海洋 / 河北生态 / HPLC 灰色，每个行星表面场景建模 + 大气粒子。

## 验收项 F：数据分析星环（阶段6）
- 待定义：90+ 小星体按 8 领域分组旋转、悬停显示项目名、点击显示详情卡片。

## 验收项 G：转场动效（阶段7）
- 待定义：陨石带、星云、彗星、星团、相机飞行曲线，转场流畅无卡顿。

## 验收项 H：技能证书空间站（阶段8）
- 待定义：高科技风格空间站模型、技能矩阵、证书展示、联系方式。

## 验收项 I：玻璃面板与详情交互（阶段9）
- 待定义：3D 玻璃面板展示项目简介、点击行星弹出详情模态框。

## 验收项 J：性能优化与验收（阶段10）
- 待定义：60fps、LOD/instancing、reduced motion、跨浏览器测试。

## 反馈/问题
- 个人站为新建项目，所有验收项随阶段推进逐步定义和验证。

## 最终结论
- 阶段1 验收通过后，进入阶段2 实施。
